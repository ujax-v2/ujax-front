# API Configuration
# Backend API Base URL
# If empty, requests will be proxied via Vite (in dev) or handled by Nginx (in prod).
# Example: http://api.ujax.com
VITE_API_BASE_URL=

# OAuth2 Configuration
# Redirect URI for OAuth callbacks
VITE_OAUTH_REDIRECT_URI=http://localhost:5173/oauth/callback
